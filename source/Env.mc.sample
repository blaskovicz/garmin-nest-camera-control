// copy to Env.mc and fill in with real values
module Env {
	var NestClientId = "nest api client id";
	var NestClientSecret = "nest api client secret";
	var NestApiProxyURI = "deployed /api root uri of https://github.com/blaskovicz/garmin-nest-api-proxy (eg: https://someapp.heroku.com/api)";
}